#include <stdio.h>
#include "LinkList.h"

#define BUFFER_SIZE 20
int main()
{
#if 1
    char buffer[BUFFER_SIZE] = {0};
    char *ptr = strcpy(buffer, "zhangsan");
    printf("buffer:%s")
    return 0;
}